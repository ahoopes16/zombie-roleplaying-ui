import React from 'react'
import { shallow } from 'enzyme'
import ListEncounters from './ListEncounters'

// TODO Figure out how to do more tests
describe('ListEncounters', () => {
  test('renders', () => {
    shallow(<ListEncounters />)
  })
})
