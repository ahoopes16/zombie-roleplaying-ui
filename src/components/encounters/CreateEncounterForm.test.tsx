import React from 'react'
import { shallow } from 'enzyme'
import CreateEncounterForm from './CreateEncounterForm'

// TODO Figure out how to do more tests
describe('CreateEncounterForm', () => {
  test('renders', () => {
    shallow(<CreateEncounterForm />)
  })
})
